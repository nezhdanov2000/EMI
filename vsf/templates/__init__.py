"""
Non-Python assets (CSS/JS/HTML) bundled for `vsf.dashboard.export_full_dashboard`.

This `__init__.py` exists only so `vsf.templates` is a real, installable
Python package (`importlib.resources.files("vsf.templates")` requires
that) — nothing in this module is meant to be imported or executed.
"""
