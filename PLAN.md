# VSF: план приведения проекта в порядок

## Ход работ (2026-09-16)

Фаза 0, сделано и закоммичено в ветку AjarnWendy (не запушено):
- b1a76f3 `.gitattributes`, LF везде (CRLF-шум ушёл, содержимое не менялось).
- af7fb65 `Claude outputs/` убрана из git (файлы на диске остались, папка в .gitignore).
- b6f1236 `benchmark_data/MANIFEST.csv` + `experiments/datasets.py` + `tests/test_benchmark_manifest.py`. **Исправлен заголовок soybean_large.csv**: первые 5 имён были заголовками разделов из .names, все имена сдвинуты на 5 колонок. Прошлые результаты с именами колонок soybean недействительны.
- 3e4de78 PMD: порядок 4.13, неверная ссылка 4.7, ложное «нет пропусков», устаревший HTTP API, число тестов.
- Тесты: 349 passed, 2 skipped на numpy 2.2.6/pandas 2.3.3 и на numpy 1.23.5/pandas 1.5.3.

Найдено по ходу:
- 2 пропущенных теста и пример из раздела 3.2 PMD требуют `data/adult_census.csv`, которого нет в репо → пример не воспроизводится. Написан `experiments/build_adult.py` (UCI, 8 категориальных колонок, самопроверка по 32561 строк / 9 Armed-Forces / 7841 >50K / 13193 Husband). С машины студента UCI недоступен из сессии (403 egress) — запустить у себя.
- Нижние границы зависимостей в pyproject (numpy>=1.20, pandas>=1.3) не тестировались; реально проверено 1.23.5 / 1.5.3.

Также сделано:
- `experiments/build_adult.py`, `data/` в .gitignore.
- `requirements-lock.txt` (numpy 2.2.6, pandas 2.3.3, pytest 9.1.1); нижние границы в pyproject подняты до реально проверенных (numpy 1.23.5, pandas 1.5.3); настройки pytest в pyproject.
- `.github/workflows/tests.yml`: 3 версии Python на фиксированных версиях; минимальные версии; отдельная задача, которая собирает Adult и требует, чтобы 2 теста не пропускались. В GitHub ещё не запускался — проверится после push.
- `experiments/README.md`, вывод `null_certificate.py` в `experiments/results/`.

Осталось из фазы 0: запушить и посмотреть CI; восстановить скрипт биннинга titanic и конвертации audiology/mushroom (или честно пометить результаты на них как невоспроизводимые).

Плиты d=5/6 — сняты с плана до конца фазы 3 (в коде их не было).

### Фаза 1 (2026-09-16)

Сделано (`vsf/selective.py`, `tests/test_selective.py`, PMD раздел 4.14):
- 1.1 Сертификат после отбора: `certify_discovery(method="family_bonferroni" | "split")`. Westfall–Young **отклонён**: перестановка проверяет независимость, а не «чистота ≤ τ»; ячейку с чистотой 0.85 он «сертифицирует» как > 0.90.
  - Шум, 100 прогонов: текущий сертификат — ложный центр хотя бы в одной ветке в 68 %; оба новых — 0 %.
  - Ячейка ровно на τ рядом с настоящим центром (строгий контроль): ложных 0 у обоих; мощность N=500: 5 % (family) и 18 % (split) против 75 % у некорректного; N=1000: 57 % / 65 %; N=3000: 100 %.
  - titanic τ=0.7: family сертифицирует женщин 1–2 класса (47 % выживших), split — ничего.
  - Проверка Хёффдинга (ячейка из разнородных строк) верна только при уровне ≤ 0.05 → `alpha > 0.05` запрещён.
- 1.2 Вложенная CV: `nested_crossvalidation`; те же фолды, что у текущей CV (парные).
- 1.3 Стабильность схемы: `schema_stability`. titanic 4D τ=0.9: победитель совпадает в 2 из 25 фолдов, 13 разных победителей.
- 1.4 Калибровка: медленный тест (300 прогонов) — частота ложных сертификатов ≤ α.
- Разобран вопрос «вложенная CV выше обычной»: это разные процедуры, нижней границей обычная не является. Гипотеза (не проверена): схема по всем данным выбрана за ячейки на грани τ, которые на фолде выпадают. Вывод: CV-покрытие без CV-чистоты не интерпретируется — теперь выводятся оба.
- В интерфейсе цифра «на всех данных» завышает воспроизводимое покрытие до 14 п.п. (titanic 4D) и 16 п.п. (car_evaluation).

Дополнено:
- Разложение разрыва «вложенная vs обычная CV» (`experiments/cv_gap_decomposition.py`): весь разрыв — это цели в ячейках, которые центры на всех данных, но не на обучающем фолде (titanic 4D: 13.7 п.п.; car_evaluation: 15.9). Простая версия гипотезы («у победителя больше ячеек на грани τ») верна в 9 из 12 случаев, не во всех.
- 1.5 В интерфейсе: Branches → «Check this result» (`/api/validate`, фоновая задача, опрос). Показывает вложенную CV с чистотой, стабильность схемы, валидные сертификаты с условиями ячеек; строка на каждой карточке ветки. В строгом режиме — предупреждение, что сертификаты решётки «на одну схему». Проверено в браузере (Playwright) для обоих методов.
- Отдельный флаг «режим публикации» не делался: его роль выполняет явная проверка.

Осталось:
- Экспорт (`export_full_dashboard`) проверку не содержит.
- Решётка по-прежнему красит по сертификату «на одну схему».

Составлено 2026-09-16. Основа — две оценки для Q1 (`claude/q1-assessment.md`) и проверка кода.

## Главное

Сначала чиним статистику, потом проводим оценку, потом пишем текст. Новые функции интерфейса замораживаются до конца фазы 3.

Первая цель — **статья A (метод, журнал уровня DMKD)**. Для неё не нужны люди: фазы 1–3 и есть содержание статьи. **Статья B (визуализация, TVCG)** — после A. Для неё нужен эксперимент с пользователями, а это ещё 2–3 месяца.

Грубая оценка: 6–10 недель полной занятости до черновика статьи A.

---

## Что уточнено по отчёту другого агента

1. **Скриптов `~/exp/nested.py` и `~/exp/nullc.py` не существует.** Они лежали во временной папке сессии и пропали. Эксперимент с шумом я повторил и сохранил в репозиторий: `experiments/null_certificate.py`. Скрипт вложенной CV нужно писать заново.
2. **С сертификатом всё хуже, чем в отчёте.** Условия: 100 прогонов, N=3000, 12 колонок по 4 уровня, доля target 0.45, τ=0.5, Rule C, α=0.05.

   | d | ложный «сертифицированный» центр у победителя | 95 % интервал |
   |---|---|---|
   | 1 | 0/100 | 0.00–0.04 |
   | 2 | 1/100 | 0.00–0.05 |
   | 3 | **36/100** | 0.27–0.46 |
   | 4 | **60/100** | 0.50–0.69 |

   Обещано не больше 5 %. При d = 3–4 гарантия не работает.
3. **Числа вложенной CV подозрительны.** Вложенная CV дала 43.8 %, а обычная — 38.6 %. Обычно честная (вложенная) оценка не выше обычной. Пока это не объяснено, опираться на эти числа нельзя. Возможная причина: правило ёмкости ⌊N/10⌋ считается на обучающем фолде иначе, и уровни сливаются по-другому.
4. **Вывод «смещение небольшое» неверен.** Разрыв уже внутри текущей CV: у titanic 4D на всех данных 52.3 %, а по CV 38.6 %. Значит, главная цифра в интерфейсе завышена на 14 пунктов.
5. **«Все 337 тестов проходят» я не перепроверил.** В системном python на машине нет pytest. Проверка встанет в CI (фаза 0).

---

## Фаза 0. Порядок в репозитории (1–2 дня)

- [ ] **Закоммитить незакоммиченную работу.** Это вкладка Trade-offs и `Landscape.frontier`: 622 строки в 8 файлах. Отдельным коммитом.
- [ ] **Починить концы строк.** Сейчас `git status` показывает изменёнными 20 файлов, включая все CSV. Всего около 59 тыс. строк «изменений», и почти всё это замена LF на CRLF. Что сделать:
  - добавить `.gitattributes` со строками `* text=auto eol=lf` и `*.csv text eol=lf`;
  - выполнить `git add --renormalize .`.
- [ ] **Убрать `Claude outputs/` из git.** Сейчас там 10 файлов. Папку внести в `.gitignore`. Использование ИИ раскрыть в статье, а не в мусоре репозитория.
- [ ] **Описать датасеты** в `benchmark_data/MANIFEST.csv`: источник (URL и версия), предобработка, SHA-256.
  - `adult_prepared.csv` и `connect4.csv` из прошлых экспериментов в репозитории отсутствуют.
  - Биннинг Adult делался вручную. Его нужно заменить скриптом.
- [ ] **Завести папку `experiments/`.** Правило: каждое число в PMD и в статье выдаёт скрипт отсюда, с фиксированным seed и выводом в CSV.
  - Переписать `compare.py` (утерян). `plates2.py` не нужен — плиты заморожены.
  - Заново написать `nested_cv.py`.
- [x] ~~Публичный API для d>4~~ — не нужен, пока плиты заморожены.
- [ ] **Настроить CI.** GitHub Actions: pytest на Python 3.10–3.12, плюс файл с фиксированными версиями зависимостей для воспроизведения.
- [ ] **Исправить мелкие несостыковки в PMD:**
  - ссылка «Section 4.7» про немонотонность ведёт не туда;
  - разделы идут в порядке 4.13 → 4.10;
  - список HTTP API в приложении устарел;
  - число тестов расходится (337 или 328).

**Готово, когда:** `git status` чистый, CI зелёный, `python experiments/null_certificate.py` воспроизводит таблицу выше.

---

## Фаза 1. Честная статистика (1–2 недели) — главная

### 1.1 Сертификат, который держится после выбора схемы

Проблема: поправка Бонферрони делается по ячейкам одной схемы, а схема выбрана из сотен и тысяч кандидатов. Реализовать три варианта и сравнить:

| вариант | как | плюс | минус |
|---|---|---|---|
| **Split (разделение)** | Схема ищется на половине A. Центры сертифицируются на половине B с поправкой α/C_B. | Гарантия точная и простая | Теряется половина данных |
| **Westfall–Young max** | Одна общая перестановка target на всё семейство. Берётся максимум статистики по всем схемам и ячейкам, из этого распределения получается поправленный порог. | Мощнее, чем split | Стоит B × (все схемы). Нужно условие subset pivotality |
| **Бонферрони по семье** | α / Σ_S C_S | Просто и точно | Очень консервативно. Нужен как нижняя граница для сравнения |

- [ ] Split сделать вариантом по умолчанию для слова «certified».
- [ ] Rule P переименовать в «descriptive» (описательный). В интерфейсе не называть его сертификатом.
- [ ] Для каждого варианта записать условия, при которых гарантия верна. Короткое доказательство — в PMD.
- [ ] Добавить медленный тест калибровки (маркер `slow`): на шуме доля ложных сертификатов ≤ α + ошибка Монте-Карло, для каждого d.

### 1.2 Вложенная кросс-валидация

- [ ] Поиск схемы запускать внутри каждого фолда. Отчёт даёт три числа: на всех данных, CV с фиксированной схемой, вложенная CV.
- [ ] Сначала разобраться, почему вложенная CV выше фиксированной (пункт 3 выше). Добавить тест на эту ситуацию.
- [ ] `select_branch_dimensionality` перевести на вложенную CV.

### 1.3 Стабильность схемы

- [ ] **Доля фолдов или bootstrap-выборок с той же схемой S\*,** плюс средний Жаккар по колонкам. На titanic 4D победитель менялся в 5 из 5 фолдов, и сейчас ни один показатель об этом не сообщает.
- [ ] **Размер «Rashomon-набора»** — сколько схем в пределах ε от победителя. Landscape это уже знает, нужен только показатель.
- [ ] Показать оба числа в панели ветки.

### 1.4 Проверка p-value

- [ ] Под шумом familywise p должен быть равномерным на [0, 1] (QQ-график). Обычный p должен быть заметно смещён. Это пункт 9.1.4 PMD.

### 1.5 Режим публикации

- [ ] Добавить флаг `publication=True`. Он включает: certified через split или Westfall–Young, вложенную CV, familywise с B ≥ 999, m ≥ 10. Поверх заданных параметров флаг ничего молча не меняет: при конфликте выдаётся ошибка.

**Точка решения после фазы 1.** Если с честным сертификатом при τ ≥ 0.9 покрытие почти везде ≈ 0, режим «certified» бесполезен на практике. Тогда главным показателем статьи становится вложенная CV, а сертификат остаётся дополнительным.

---

## Фаза 2. Скорость (около 1 недели)

- [ ] **Отсечение по верхней оценке** (optimistic estimate). Это исследовательская задача, а не готовый рецепт.
  - Идея: при Rule C ячейка-центр должна содержать не меньше k\* позитивов (при τ=0.9 и α=0.05 это 29). Значит, крупная ячейка с k < k\* не даст центров ни в одном уточнении.
  - Сложность: правило ёмкости ломает вложенность ячеек (11.9 % пар на titanic). Слияние уровней может создать центр, которого нет в неслитом варианте. Оценку нужно доказать с учётом этого.
  - Каждое отсечение закрепить тестом «тот же ответ, что полный перебор», как в `test_fastpaths.py`.
- [ ] **Параллельный перебор** по кандидатам (multiprocessing).
- [ ] **Замеры времени** по N, M, d и числу перестановок B. Сохранять в CSV — это готовая таблица для статьи.

---

## Фаза 3. Оценка (2–4 недели)

### 3.1 Данные

- [ ] Составить список из 20–30 категориальных датасетов (OpenML, PMLB) **до** запуска и зафиксировать его. Критерии отбора записать заранее.
- [ ] Включить наборы с M ≥ 50 и N ≥ 50 тыс.
- [ ] Загрузка — скриптом, с проверкой хэша.

### 3.2 Бейзлайны (при одинаковых τ, m, длине правила ≤ d и числе элементов на экране)

| бейзлайн | зачем |
|---|---|
| Полный перебор правил длиной ≤ 4 (`compare.py`) | Верхняя граница: сколько теряет требование «одна сетка» |
| **Дерево решений глубины ≤ 4** (листья с чистотой ≥ τ = центры) | Самый естественный конкурент. Если дерево выигрывает при том же числе условий, вклад A под вопросом |
| pysubgroup (beam / best-first) | Стандартный инструмент поиска подгрупп |
| SSD++ (Proença et al., 2022) | Современный поиск подгрупп со статистической строгостью |
| Жадный выбор колонок | Проверка заявления C1: как часто жадный путь отличается от лучшего и насколько проигрывает |

Метрики:
- вложенная CV-оценка покрытия;
- K и число условий d×K;
- доля ложных сертификатов;
- стабильность схемы;
- время.

### 3.3 Синтетика с известным ответом

- [ ] Подсаженный узор, который ложится в сетку. Метод должен его найти.
- [ ] XOR-синергия. Метод должен найти пару, жадный путь — нет.
- [ ] **Узор, который НЕ ложится в сетку** (правила на разных колонках). Это прямой замер «цены выравнивания».
- [ ] Полный шум при разных N, M, доле target и числе уровней.

### 3.4 Абляции

- [ ] Правило ёмкости: N/10, N/5, N/20, без правила.
- [ ] Правило слияния номинальных уровней.
- [ ] Порог m.

### 3.5 Кандидаты в главный результат

1. **Цена выравнивания:** потеря покрытия у одной сетки против свободных правил в зависимости от K и d.
2. **Связь τ и d:** оптимальное d растёт с τ, покрытие немонотонно по d. Проверить на 20+ датасетах, а не на 12 случаях.
3. **Нестабильность схем** при d = 3–4 и то, как её показывать.

**Точка решения после фазы 3.** Если дерево глубины 4 или SSD++ заметно выигрывают по вложенной CV при равном d×K, статья A не получится. Тогда ограничение «одна сетка» оправдывает только визуализация, и путь один — статья B с экспериментом на пользователях.

---

## Фаза 4. Текст (1–2 недели)

- [ ] **Разделить PMD и статью.** PMD остаётся спецификацией кода, статья пишется отдельно и с нуля, своими словами.
- [ ] **Один вклад, не больше трёх пунктов.** Убрать из списка вкладов: C2 (известный факт), C3 (это ограничение), C9 (это UX).
- [ ] **Переписать связанные работы** (раздел 7): поиск подгрупп (SD-Map, CN2-SD, SSD++, van Leeuwen & Knobbe), статистически корректный поиск закономерностей (Hämäläinen & Webb, LAMP), безопасное визуальное исследование данных (Zhao et al. 2017, Safe Visual Data Exploration), срезы данных (Slice Finder).
- [ ] Раскрыть использование ИИ по политике журнала.
- [ ] Выпустить версию с тегом и получить DOI на Zenodo. Написать раздел о доступности кода и данных.

## Статья B (после A, по желанию)

- [ ] Протокол эксперимента с пользователями, одобрение этического комитета, пилот.
- [ ] n ≥ 40, сравнение со списком правил или таблицей pysubgroup. Задачи и метрики зафиксировать заранее.
- [ ] Вклад статьи: дисплей, Landscape и Trade-offs как способ показать цену описания и нестабильность.

## Заморожено до конца фазы 3

- Плиты d = 5/6.
- Новые вкладки и функции интерфейса.
- Исключение: всё, что нужно для показа стабильности и честного сертификата.
